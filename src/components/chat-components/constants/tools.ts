/**
 * List of available tools for the chat interface
 * This is the source of truth for all tool references
 */
export const AVAILABLE_TOOLS = ["@vault", "@websearch", "@web", "@composer", "@memory"];

/**
 * Tools that should be available for @ mention autocomplete in non-Plus mode.
 */
export const FREE_MODE_AVAILABLE_TOOLS = ["@websearch", "@web"];
